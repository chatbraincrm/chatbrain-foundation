# =============================================================================
# CHATBRAIN — Variáveis de ambiente (copie para .env ou .env.local)
# =============================================================================
# Nada depende do Lovable Cloud. Tudo roda com Supabase + .env local.
# Segredos nunca no frontend (apenas VITE_* publicáveis).
# =============================================================================

# -----------------------------------------------------------------------------
# FRONTEND (Vite — build e dev)
# Usado pelo app React; exposto no bundle (não coloque segredos aqui).
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=https://seu-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Opcional: URL base da API quando o front e a API rodam em origens diferentes.
# - Com Docker (nginx na 8080): deixe vazio → usa same origin (http://localhost:8080/api)
# - Dev com server na 3001 e Vite na 8080: use VITE_API_URL=http://localhost:3001
VITE_API_URL=

# -----------------------------------------------------------------------------
# SERVIDOR (Node — webhook WhatsApp + API de convites)
# Usado pelo container api e por `npm run server`. Nunca no frontend.
# -----------------------------------------------------------------------------
SUPABASE_URL=https://seu-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIs...
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# URL pública do app (links de convite e redirects). Ex.: http://localhost:8080 (Docker) ou https://app.seudominio.com
APP_BASE_URL=http://localhost:8080

# Email (convites)
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@seudominio.com

# WhatsApp Evolution (opcional)
# Credenciais podem vir do env (fallback) ou da UI (salvas por tenant no banco).
EVOLUTION_BASE_URL=
EVOLUTION_API_KEY=
EVOLUTION_WEBHOOK_SECRET=
OPENAI_API_KEY=
ENABLE_WHATSAPP_OUTBOUND=false
